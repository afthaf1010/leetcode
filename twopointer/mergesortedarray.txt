class Solution(object):
    def merge(self, nums1, m, nums2, n):
        i=m
        j=0
        for v in range(m,len(nums1)):
            nums1[i],nums2[j]=nums2[j],nums1[i]
            i+=1
            j+=1
        return nums1.sort()
