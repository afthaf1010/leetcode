class Solution:
    def shortestToChar(self, s: str, c: str) -> List[int]:
        a=[]
        for i in range(len(s)):
            b=len(s)
            for j in range(len(s)):
                if s[j]==c:
                    b=min(b,abs(i-j))
            a.append(b)
        return a     
